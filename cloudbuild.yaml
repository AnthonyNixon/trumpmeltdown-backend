steps:
- name: 'gcr.io/cloud-builders/go'
  args: ['get', '-d', './...']
  env: ['GOPATH=/gopath']
- name: 'gcr.io/cloud-builders/go'
  args: ['install', 'main.go']
  env: ['GOPATH=/gopath']
artifacts:
  objects:
    location: 'gs://ajnhosting-binaries/trumpmeltdown'
    paths: ['trumpmeltdown-backend']
